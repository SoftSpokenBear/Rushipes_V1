@model List<RushipesWeb_V1.Models.Ingredient>

<div class="card shadow border-0 my-4">
    <div class="card-header primary-background-color-site ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Liste des Ingrédients</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3 justify-content-center">
            <div class="col-md-6">
            </div>
            <div class="col-md-6 text-end">
                <a asp-controller="Ingredient" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Créer
                </a>
            </div>
        </div>


        <table class="table table-bordered table-striped text-center">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nom</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td>@obj.Id</td>
                        <td>@obj.Name</td>

                        <td class="align-middle">
                            <a asp-controller="Ingredient" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-info mx-2">
                                <i class="bi bi-pencil-square"></i> Modifier
                            </a>
                      
                        </td>
                    </tr>
                }

            </tbody>
        </table>

    </div>
</div>
